hw02a.py:
  Print the numbers from 1 to 5
  using a for loop
  using a while loop
hw02b.py:
  Print the numbers from one to 20 along with whether each number is even or odd and prime or composite. 
  Use a function isEven(x) which returns True if a number is even and False otherwise.
  Use a function isPrime(x) which returns True if a number is prime and False otherwise.
  Determine if a number is prime using a loop.
  Loop through the numbers 1 to 20 and for each number print:
  <number>\t<"even" or "odd">\t<"prime" or "composite">\n
hw02c.py:
  Write a program to identify a component based on a character as would be used in a SPICE netlist
